---
title: "Python parameter names"
publish: false
tags: ["Python"]
---
# Python parameter names

変数はただの名前だ。名前はオブジェクトを指しているので、名前に代入されている値を変えれば、名前は新しいオブジェクトを指すようになる。

名字是类似指针一类的东西，如下的a和b指向了同一个list，而该list是mutable的所以可以通过a[1]的切片来使得a[1]重新指向新的值。
