
第一阶段 = clone BasicSR + editable install + 只改数据和 yml 做 baseline，对应「全仓库 + 安装模式 + init-ai 场景四」。
效果不好时，优先查数据和配置、换官方 model/预训练，不要先换仓库，更不要先改网络；你暂时没改网经验完全正常，把 yml 和 data pipeline 玩熟就够用第一阶段了。


---

#### ステップ 1：古いドライバのクリーンアップによる依存関係エラーの解消
    
    ```
    sudo apt-get remove --purge '^nvidia-.*'
    sudo apt-get remove --purge '^libnvidia-.*'
    sudo apt-get autoremove
    ```
    
- **理由：** 新しいドライバのインストール時に依存関係の競合（held broken packages）が発生したため。システムの破損（いわゆる「OSが落ちる」状態）を防ぎ、高バージョンのドライバをクリーンに導入するには、古いドライバ一式と関連ファイルを完全にアンインストールする必要がありました。
    

#### ステップ 2：データセンター向け 570-Server ドライバのインストールと再起動

    ```
    sudo apt install nvidia-driver-570-server
    sudo reboot
    ```
    
-  4枚の V100 というエンタープライズ向けの計算リソースに対しては、最新の通常版よりも、长期サポート（Production Branch）である `-server` 安定版を選択する方が堅牢です。これにより、マルチGPUによる高負荷な学習時の「GPU認識エラー（显卡掉线）」を防ぎます。また、新しいカーネルモジュールを読み込ませるためにソフトリブート（`reboot`）が不可欠でした。
    

#### ステップ 3：NVIDIA Container Toolkit（架け橋）の設定
    
    ```
    # 公式リポジトリ設定後、ツールをインストールし、以下を実行
    sudo nvidia-ctk runtime configure --runtime=docker
    sudo systemctl restart docker
    ```
    
-  コンテナ運用の原則は「ホスト側にはドライバのみ、CUDA環境はコンテナ内に構築する」です。しかし、デフォルトの Docker は GPU を認識できないため、4枚の V100 をコンテナへ透過的に割り当てる（パススルーする）ための架け橋として Container Toolkit の導入が必要です。
    

#### ステップ 4：環境検証と Docker バージョンの確認
    
    ```
    docker --version
    sudo docker run --rm --gpus all nvidia/cuda:11.8.0-base-ubuntu20.04 nvidia-smi
    ```
    
-  Docker のバージョンが `--gpus` オプションに対応している（19.03以上である）ことを確認するためです（他ユーザーのタスク中断を防ぐため、Docker 本体のアップデートは敢えて見送りました）。最終的に、テスト用コンテナ内で `nvidia-smi` の出力に成功したため、GPU パススルー環境の構築完了を確認できました。今後は安心して Dockerfile の作成に移行できます。 """
    


---
### 1. プロジェクトの位置づけ

- AI コーディング規約とMLOps（Docker / GitLab CI / GPU 学習）を一体にした再利用可能なテンプレートを整備しました。
- 新規・既存プロジェクトでは `init-ai` 一コマンドで、Cursor ルール・品質チェック・CI/CD・Docker 設定をまとめて注入できます。

> 本リポジトリは、AI 開発ルールと MLOps パイプラインを標準化した社内テンプレートです。メンバーはプロジェクトを pull し `init-ai` するだけで CI/CD と Docker ベースの開発環境の骨格が揃い、Windows でも Docker 対応 PC なら個別セットアップを大幅に削減できます。GPU 学習は gpu-server に集約し、GitLab からワンクリックで実行できるようにしました。

---

### 2. 主な成果（技術面）

|領域|内容|
|---|---|
|テンプレート注入|`install.sh` / `inject-ai.sh` により `~/.ai-coding-rules` から一括セットアップ|
|品質管理|Ruff / Pyright / pre-commit；GitHub Actions と GitLab CI の quality job|
|MLOps|`Dockerfile`、`.devcontainer`、`train.py`（GPU スモークテスト）|
|GitLab CI|`.gitlab-ci.yml` + `.gitlab/ci/quality.yml` + `.gitlab/ci/train.yml`|
|GPU 学習|自前 Runner（tag: `gpu-server`）で `docker build/run` により学習コンテナを起動|
|ドキュメント|README（英中）+ `docs/MLOPS-CHECKLIST`（验收手順）|
|運用|GitHub / GitLab 双方向 mirror；CI 障害（Pyright / Runner executor）の修正と YAML 内コメント整備|

---

### 3. 価値（Windows 環境を含む）

（Windows）・非 GPU メンバー向け

- 本番 GPU 学習は gpu-server 上の GitLab Runner が担当。Windows PC 上で GPU や Docker を深く触る必要はありません。
- コード品質チェック（Ruff / Pyright）は GitLab / GitHub の CI が自動実行。ローカルに Python 環境を揃えなくても、push すれば結果を Web で確認できます。
- 開発時は Cursor + Remote SSH / Dev Containers を想定。Windows でも Docker Desktop + ドライバが要件を満たせば、リポジトリを pull するだけで、チーム共通のコンテナ環境に入れます（手動で uv / Ruff / Pyright を都度設定する必要を減らせます）。
- 新プロジェクト参加時は `init-ai` 一発で、AI ルールと CI/CD テンプレが揃い、属人化した「環境構築メモ」依存を軽減できます。

GPU サーバー担当（gpu01 等）向け

- push 後、GitLab 上で Play ボタン（または commit `[run train]`）で学習ジョブを起動。各サーバーへ SSH して pull し直す運用を減らせます。
- Runner は `shell` executor + tag `gpu-server` が前提（設定済みであれば、学習用 Docker はプロジェクトの `Dockerfile` に従います）。

チーム全体

- テンプレート 1 本を更新 → `git pull` → 各プロジェクトで `init-ai --update` により、ルールと CI 設定の横展開が可能です。
- 詳細手順は `docs/MLOPS-CHECKLIST.zh-CN.md` に集約（README は概要のみ）。

---

### 4. 現在の運用フロー（イメージ）

開発者（Mac / Windows + Cursor）

→ リポジトリ clone / pull

→ init-ai（初回のみ）または通常開発

→ push → GitLab CI（quality 自動 / training は manual または [run train]）

→ gpu-server が学習コンテナを起動

---

### 5. 残タスク・前提

- gpu-server 側：Docker、NVIDIA Container Toolkit、Runner（shell executor） の完了が前提。（）
- `run_training` の CI 成功は 「コンテナ起動まで」；学習成否は `docker logs` 等で別途確認。

---


# BasicSR ローカル環境構築

## 概要

`/home/huang/code/cst_ai/BasicSR` に、公式リポジトリ [XPixelGroup/BasicSR](https://github.com/XPixelGroup/BasicSR) を clone し、GPU サーバー上で開発・学習可能な環境を構築した。upstream の構成（`setup.py` / `requirements.txt` / `basicsr/` / `options/`）は維持し、AI 開発テンプレート（`init-ai`）を追加注入した。

## デプロイ内容

|項目|内容|
|---|---|
|配置先|`/home/huang/code/cst_ai/BasicSR`|
|ソース|公式 upstream（fork なし）|
|Python 環境|`uv venv --python 3.11`（CPython 3.11.14）|
|PyTorch|CUDA 12.4 版（cu124 wheel）|
|BasicSR|`uv pip install -r requirements.txt` + `uv pip install -e .`（v1.4.2）|
|GPU|Tesla V100 × 4、Driver 570 / CUDA 12.8|

### 追加注入（`init-ai`）

- Cursor / Claude 用ルール（`.cursor/rules/`、`CLAUDE.md` 等）
- プロジェクトコンテキスト（`.cursor/project-context/overview.md`）
- Lint / 型チェック設定（Ruff、Pyright、pre-commit 設定ファイル）
- CI / MLOps テンプレート（`.gitlab-ci.yml`、`Dockerfile`、`.devcontainer/`）
- ルート `train.py`（MLOps スモークテスト用。BasicSR 本体の学習入口は `basicsr/train.py` のまま）

### 本機向け調整

- `.devcontainer/devcontainer.json`：`postCreateCommand` で BasicSR 依存を自動インストール
- `.gitlab/ci/train.yml`：データマウント `/mnt/data`、学習コマンドを BasicSR 形式に設定

## 検証内容

### 1. ホスト venv

|検証項目|結果|
|---|---|
|`import basicsr`|OK（v1.4.2）|
|CUDA 認識|OK（`cuda: True`、GPU 4 枚）|
|`basicsr/train.py --help`|OK|
|`basicsr/train.py` の保持|OK（上書きなし）|

### 2. Docker GPU スモークテスト

docker build -t basicsr-mlops-smoke .

docker run --rm --gpus all --shm-size 16g \

-v $(pwd):/workspace -v /mnt/data:/data \

basicsr-mlops-smoke bash -lc "python train.py"

|検証項目|結果|
|---|---|
|コンテナ起動|OK|
|`CUDA available`|`True`|
|GPU デバイス|Tesla V100-PCIE-32GB|
|データマウント `/data`|available|
|スモーク学習ループ（5 epoch）|正常完了|

## 学習の起動方法（参考）

cd /home/huang/code/cst_ai/BasicSR

source .venv/bin/activate

PYTHONPATH=./ CUDA_VISIBLE_DEVICES=0 \

python basicsr/train.py -opt options/train/<タスク>/<設定>.yml

## 未実施・今後の作業

- 実データを用いた本番学習（データセット配置・yml 設定が必要）
- GitLab `gpu-server` Runner 登録（CI 経由の学習スケジュール）
- 特定タスク向け `options/train/*.yml` の選定・カスタマイズ


---

