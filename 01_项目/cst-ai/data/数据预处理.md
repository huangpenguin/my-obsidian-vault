我现在需要在这个文件夹里准备数据，所以相关的预处理脚本就现在这里进行简单的数据预处理。

这是tiff的x光照片数据，请你按照全局固定范围归一化（当然你有更好的建议比如对于cylinder有一个更好的范围可以屏蔽图中的x光线则更好），保留尽可能宽泛且未被截断的物理密度数据（通过我们上个回答提到的全局线性归一化），让含有伪影和噪声的完整数据进入网络，让 SwinIR 强大的注意力机制自己去学习如何分离信号与噪声。