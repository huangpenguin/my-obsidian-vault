---
title: "git branch"
publish: false
tags: ["Git"]
---
# git branch

Git 的 `branch` 功能用于在项目中创建和管理不同的开发分支，允许在不影响主分支的情况下进行并行开发。以下是其主要用途和使用方法：

### 用途

1. **并行开发**：多个开发者可以在不同分支上同时工作，互不干扰。
2. **功能隔离**：每个新功能或修复可以在独立分支上开发，完成后合并到主分支。
3. **版本管理**：通过分支管理不同版本，便于维护和发布。
