---
title: "数学运算"
publish: false
tags: ["Python"]
---
# 数学运算

- 元素级运算：(element-wise)
    
    ```
    arr1 = np.array([1, 2, 3])
    arr2 = np.array([4, 5, 6])
    print(arr1 + arr2)  # 输出: [5 7 9]
    print(arr1 * arr2)  # 输出: [4 10 18]
    ```
    
- 矩阵乘法：
    
    ```
    arr1 = np.array([[1, 2], [3, 4]])
    arr2 = np.array([[5, 6], [7, 8]])
    dot_product = np.dot(arr1, arr2)
    print(dot_product)
    ```
    
- 求和：
    
    ```
    arr = np.array([[1, 2], [3, 4]])
    print(np.sum(arr))  # 所有元素求和，输出: 10
    print(np.sum(arr, axis=0))  # 沿列求和，输出: [4 6]
    print(np.sum(arr, axis=1))  # 沿行求和，输出: [3 7]
    ```
    
- 平均值：
    
    ```
    print(np.mean(arr))  # 所有元素的平均值
    ```
    
- 最大值/最小值：
    
    ```
    print(np.max(arr))  # 最大值
    print(np.min(arr))  # 最小值
    ```
