---
title: "引号相关"
publish: false
tags: ["Python"]
---
# 引号相关

如果使用单引号’，则字符串内部可以放入双引号，反之亦然。

使用三引号可以进行多行输入,在interpreter下，如果输入了左三引号，那么prompt会持续获取输入
，包括换行符，直到遇到右换行符

print()函数会将首位的所有引号去除，同时追加空格、换行等操作，面向人类的输出。
