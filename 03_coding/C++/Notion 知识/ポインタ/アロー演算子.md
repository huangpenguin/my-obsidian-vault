---
title: "アロー演算子"
publish: false
tags: ["C++"]
---
# アロー演算子

```cpp
struct student {
		char name[50];
		int height;
		double weight; 
}
struct student *p;//此时p为struct student *型
struct student takao={"takao",173};
//p=&takao;
(*p).height=180;//注意这里需要使用间接参照，同时注意计算优先级
p->height=180;//与上面同样结果

```
