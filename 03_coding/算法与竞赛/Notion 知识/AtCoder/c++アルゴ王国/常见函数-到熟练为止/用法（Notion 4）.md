---
title: "用法"
publish: false
tags: ["待整理"]
---
# 用法

```cpp

#include <map>using namespace std;

map<string, int> mp;  // 声明一个映射，键是字符串，值是整数
mp["apple"] = 3;  // 插入键值对
int value = mp["apple"];  // 访问键对应的值

```
