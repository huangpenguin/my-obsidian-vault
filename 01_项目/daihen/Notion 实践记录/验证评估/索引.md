---
title: "验证评估"
publish: false
tags: ["OCR","项目实践"]
---
# 验证评估

---

```jsx
tmux attach -t paddlex-train
cd /mnt/home/huang/workspace_daihen
source /home/huang/miniconda3/bin/activate paddle_env310
python /mnt/home/huang/workspace_daihen/third_party/PaddleX/main.py \
    -c /mnt/home/huang/workspace_daihen/third_party/PaddleX/paddlex/configs/modules/text_detection/PP-OCRv5_server_det.yaml \
    -o Global.mode=evaluate \
    -o Global.dataset_dir=./datasets/train_data/det \

```

```jsx
#rec
python /mnt/home/huang/workspace_daihen/third_party/PaddleX/main.py \
-c /mnt/home/huang/workspace_daihen/third_party/PaddleX/paddlex/configs/modules/text_recognition/PP-OCRv5_server_rec.yaml \
    -o Global.mode=evaluate \
    -o Global.dataset_dir=./datasets/train_data/rec \
```

[指标](%E6%8C%87%E6%A0%87.md)

---

## 整合后

```
conda activate paddle_env310

python3 scripts/eval_model.py \
  -c output/rec_data_all/baseline/config.yaml \
  -w output/rec_data_all/baseline/best_accuracy/best_accuracy.pdparams

python3 scripts/eval_model.py \
  -c output/rec_data_10x/baseline_mobile/config.yaml \
  -w output/rec_data_10x/baseline_mobile/best_accuracy/best_accuracy.pdparams
  
  
  
```

---

test推理，目视效果

```
conda activate paddle_env310

# 推理rec_data_all模型的test集前20个
python scripts/test_infer.py output/rec_data_all/baseline datasets/train_data/train_aug_all 20

# 推理rec_data30x模型的test集前50个
python scripts/test_infer.py output/rec_data30x/baseline datasets/train_data/train_aug_30x 50

# 推理更多样本
python scripts/test_infer.py output/rec_data_all/baseline datasets/train_data/train_aug_all 100

python scripts/test_infer.py output/rec_data_all/baseline_mobile datasets/train_data/train_aug_all --num-samples 1000
```

---

streamlit可视化

```jsx
conda activate paddle_env310 && streamlit run scripts/testing/streamlit_app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true

# 3. 访问
打开浏览器: http://localhost:8501
```
