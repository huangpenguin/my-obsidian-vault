# 博客发布文件头（复制此代码块）

在 Obsidian 中新建或打开一篇已经脱敏的笔记：将下面的**整个代码块内容**复制到文件第 1 行，再填写实际内容。不要复制代码块外的说明文字。

```yaml
---
title: "文章标题"
aliases: []
tags: []
category: ""
published: "2026-07-30"
updated: "2026-07-30"
draft: false
publish: false
---
```

发布前将 `publish: false` 改成 `publish: true`，然后正常提交并推送笔记库。只有该值**严格为 `true`** 的笔记才会同步到博客。

- 脱敏、校对中：保持 `publish: false`。
- 临时不想在网站显示：改成 `publish: false`；下一次同步会从网站撤下。
- `draft: true` 只用于已同步但要在生产站隐藏的文章；它不能替代 `publish: false`。
