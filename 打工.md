工资相关
1.聊之前京大的帮忙和东大的帮忙；2000的时薪吧现在；
2.比想象中开发的时间会短一点，因为有ai的帮忙，基本上开发是把框架确定开发流程和代码规范敲定，然后调查、沟通和反复调试debug、以及最重要的不断更新需求的时间可能比较长；
3.ai的token的费用就直接换算成时间，就不额外请求经费了


6.19 3.5h
6.20 1.0h
6.21  1.0h
6.24 4.5h
6.27 3.0h

你现在是一位精通 Vue3、Vite、TypeScript 的资深前端架构师和全栈开发专家。请先参考 `.cursor/project-context` 下的前端架构和项目概览（如有不妥之处请自动优化调整）。
我现在的任务是完成一个基于日本税务和劳务制度的「教员薪资系统」的功能开发、接口对接与测试。由于我经验有限，请你一步步引导我，并严格遵守以下设计需求输出代码和架构方案：

【1. 角色与权限设计（RBAC）】
- 角色设定：目前仅有「超级管理员」和「教师」两个角色。
- 架构要求：采用共用组件和页面的原则，通过角色权限（Role-Based Access Control）进行动态渲染。教师登录时，隐藏管理类操作按钮（如新增、全员调整等），且数据层面只请求和渲染其自身的数据。预留高扩展性，以便未来增加“财务”或“教务”角色。

【2. 复杂数据表格与双模式编辑（核心难点）】
- 模式切换：实现 `Overview`（概览只读模式，强调数据可视化、流畅滚动和快速筛选）与 `Layout`（布局/编辑模式）的无缝切换。
- 高度自定义表头：在 Layout 模式下，管理员除了能看到规定的表头外，还可以动态追加列（如添加多个“備考”列）。
- 动态公式列：支持用户在前端动态追加一列，并利用已有的后端数据字段（如基本给、交通费等）写入简单公式（加减乘除）生成新数据行。
- 复杂展示：表头需要是多级表头（分组归类显示）。面对长表格，必须支持横向和纵向的虚拟滚动（Virtual Scrolling）以保证性能。顶部提供筛选菜单（Bar），管理员可自由切换查看不同分类，教师默认锁定为自身数据。
- 建议组件库：请使用支持虚拟滚动和高度自定义的表格库，强烈推荐引入 `vxe-table`。

【3. 适配日本税法与劳务制度的薪资数据结构】
请在初始化状态、TS Interface 和 Mock 数据中，严格按照日本现行税法设计以下两类教员的字段（非常重要）：
- 非常勤（兼职/时薪讲师）：
  税务特点：课税区分影响极大，排班灵活。
  核心字段：社員番号、氏名、課税区分（甲欄/乙欄 - 决定源泉税率）、出勤日数、総労働時間、基本給（時給×工时）、非課税交通費（限额内免税）、源泉所得税、差引支給額（到手薪资）。（预留逻辑：若周工时超20小时需计算雇用保険料）。
- 常勤（全职员工）：
  税务特点：固定月薪，扣除项复杂（各类社保与税金）。
  核心字段：
  - 基礎：社員番号、氏名、年齢（判断40岁以上需扣介護保険）。
  - 支給：基本給、役職手当、住宅手当、通勤手当。
  - 控除（法定）：健康保険料、厚生年金保険料、雇用保険料、介護保険料、源泉所得税、住民税。
  - 勤怠：所定労働日数、出勤日数、有休消化、残業時間。
  - 汇总：支給合計、控除合計、差引支給額。

【4. 工程化与解耦设计】
- 遵循高内聚、低耦合原则：将 UI 展示层（Vue Components）、状态与缓存（Pinia）、API 数据层（Axios Services）和复杂计算逻辑（Hooks/Utils）严格分离。
- 请先输出系统的目录结构规划，以及核心表格组件和 TS Interface 的基础代码，帮助我 review 整体的搭建逻辑。
#### 最优设计建议（避坑指南）

- **关于表格选型**：普通的 Element Plus 或 Ant Design 表格在处理多级表头 + 几百行数据 + 动态追加列时会非常卡顿。**强烈建议你让 Cursor 使用 `vxe-table`**，这是 Vue 生态里处理复杂/财务表格的天花板，原生自带虚拟滚动和高级编辑功能。
    
- **关于动态公式**：前端不要自己去手写 eval() 来解析公式（有安全和报错风险）。可以让 Cursor 引入 `mathjs` 这个轻量库，将管理员输入的 `(基本給 + 通勤手当) * 0.1` 这种字符串安全地解析出结果。
    
- **关于解耦**：把日本税法的计算公式（比如甲栏乙栏的税率判定、社保比例）单独抽离到一个 `utils/taxCalculator.ts` 中，不要写在 Vue 页面文件里。






8.包含一个导出为 Excel 的按钮



