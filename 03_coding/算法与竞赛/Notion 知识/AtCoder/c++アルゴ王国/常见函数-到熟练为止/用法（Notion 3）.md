---
title: "用法"
publish: false
tags: ["待整理"]
---
# 用法

```cpp

#include <utility>using namespace std;

pair<int, int> p;  // 声明一个包含两个整数的对
p = make_pair(1, 2);  // 创建一个对
int first_value = p.first;  // 访问第一个值
int second_value = p.second;  // 访问第二个值
```
