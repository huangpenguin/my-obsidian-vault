1.action trunk
1. **快速 Baseline**：
    
    直接用 LeRobot 的 ACT 跑通比赛给的 MuJoCo 环境，**确认整条 pipeline（数据加载 $\rightarrow$ 训练 $\rightarrow$ 评估）是畅通的**。
    
2. **结合大模型/VLA（进阶）**：
    
    当你准备引入 OpenVLA 等强力模型时，借用 LeRobot / ACT 中关于 **Dataset Chunking 提取** 和 **Temporal Ensembling 推理平滑** 的几行核心代码，拼接到你的 VLA 后端上。

2.胜出者的通用套路

分析过往这类比赛的冠军方案，你会发现绝大多数获胜队伍的思路非常一致，你可以直接拿来参考：

1. **模态特征解耦与预训练 Backbone 迁移**：
    
    - 不会重头训视觉网，大部分队伍会选择现成的 Vision Backbone（如 DINOv2、SigLIP）或直接拿 OpenVLA / Octo 等预训练好的 VLA 模型基座作为起点。
        
2. **数据清洗与“数据制造”**：
    
    - 官方给的数据里面包含不少机械臂“无效抖动”或轨迹冗余。冠军队伍通常会写脚本把 Trajectory 过滤降噪，或者用 Smoothing 算法（如 Spline 插值）重新拟合轨迹。
        
3. **引入动作分块（Action Chunking）+ 历史帧拼接**：
    
    - 几乎所有靠前的队伍都会改动原生 VLA 的单步输出，采用 **Action Chunking（一次预测 8~32 步）**。同时，在 Observation 输入里**不仅仅给当前帧图像，还会拼接过去 1~2 帧的历史图像**，给模型补充“速度”和“运动方向”的信息。
        
4. **LoRA + 分层微调**：
    
    - 训练时，冻结 Vision Encoder 前半部分，只微调 LLM/VLM 层的 LoRA 权重，并重头高学习率训练 Action Head。

建议直接去 GitHub 搜索 **`CVPR Embodied AI Challenge solution`** 或 **`ManiSkill Challenge winner code`**，里面很多 Pipeline 和训练脚本能够提供直接参考。


---

测试
```
cd /home/huang/scripts/airoa-contest/PARC2026_pre

source env.sh

# 终端1

python submission_template/policy_server.py --port 8000

# 终端2

python -m pipeline --server-url http://localhost:8000 --track track1 --n-episodes 1 --max-steps 50
```

---
环境
现在是两套环境，刻意分开的：

||评测环境|训练/推理环境|
|---|---|---|
|路径|`PARC2026_pre/venv`|`workspace/training/venv`|
|启用方式|`cd PARC2026_pre && source env.sh`|`source workspace/training/venv/bin/activate`|
|Python|3.10|3.12|
|Torch|CPU（官方 setup 钉死）|CUDA（本机 2080 Ti）|
|用途|`pipeline` / `validate_submission.py` / LIBERO 仿真评测|`lerobot-train`、加载 SmolVLA、跑 `policy_server.py`|

`PARC2026_pre` = 评测环境（对齐主办方正式打分镜像那套依赖），不是训练环境。

---
本地 T1 example 只有 4 个任务（见 `compe/t1/T1_TASKS.csv`）：

| #   | 套件      | 扰动   | 指令大意        |
| --- | ------- | ---- | ----------- |
| 1   | spatial | 背景纹理 | 柜子上层黑碗 → 盘子 |
| 2   | object  | 背景纹理 | 番茄酱 → 篮子    |
| 3   | object  | 光照   | 牛奶 → 篮子     |
| 4   | goal    | 光照   | 碗 → 炉灶      |


---
# 数据

## 比赛阶段（官方结构）

|阶段|大致时间|评什么|
|---|---|---|
|予選（你们现在）|~7末–8中|提交 zip + 报告；Omnicampus 上跑 Track1 风格 评测（example 4 任务公开，正式分含未公开任务）|
|1回戦 ≈ Track1|8中–9中|同一类 primitive 任务，视觉/物理改纹理、光照等|
|2回戦 ≈ Track2|9中–10中|同一类任务，但机器人/物体位姿、视角、噪声等更强未知域|
|3回戦 ≈ Track3|10中–11中|已知 skill 的新组合（如 Open→Pick），语言指令组合|
|最終評価|11中|综合|

提供的学习数据主线是 Franka / LIBERO-plus 一类仿真演示（`lerobot/libero_plus` + 官方基线权重）。各回战不会另发一套全新真实机器人数据；变的是评测分布（域偏移 / 任务组合）。

---

## 各阶段「数据」与推荐处理

### 现在：予選 / Track1（你的第一阶段）

会用到的数据

- 训练：`libero_plus` 里与 4 个 core 对齐的演示 + 你们加的 related（可选）
- 本地自检：`T1_TASKS.csv` 那 4 个 example（纹理/光照扰动）
- 正式分：同类但未公开的任务变体（所以 related 仍有泛化价值，但不是「必须洗干净」）

推荐（同意你现在不洗）

- 不洗仿真 demo：合理。这些是成功轨迹，纹理/眩光本身就是 Track1 要扛的域随机，洗掉反而丢鲁棒性。
- 优先保：推理闭环（`n_action_steps=5`）、提交环境（py3.10 / vendor）、max_steps=600、指令 alias。
- related 可留作「免费正则」；07 这种对冲任务错一点，对 4 个 example 影响有限，不必为它单独洗库。
- 精力放在：Omnicampus 能跑、本地 4 任务稳、报告写清方法。

### Track2（未知域更强）

数据还是同一池，但评测更难（位姿/视角/噪声）。

推荐

- 仍可不做「语义清洗」，但可做 采样再平衡：难场景、长 horizon、易空抓任务多抽一些。
- 加强 域随机相关 的 demo 权重（光/纹理变体），而不是删「看起来丑」的成功集。
- 若某类系统失败（如「炉上拿碗」），再针对性加样本或对比实验——仍是加/重采样，不是大清洗。

### Track3（skill 组合）

需要的是覆盖 primitive 的广度，以及是否见过「组合语言」。

推荐

- 扩大任务覆盖（open/pick/place/stove 等 skill），而不只 4 个 T1 句式。
- 可考虑 语言增强（同义改写、组合模板），比删 episode 更重要。
- 那时若发现 demo 里真有「指令写 A、动作做 B」的脏标签，再局部丢掉才有意义。


### 评测时谁干什么

评测端 pipeline（有 LIBERO / MuJoCo）

→ 按 BDDL 建 OffScreenRenderEnv

→ reset：把 language 发给你的 /reset

→ 每步把图像+关节等 obs 发给 /act

→ 用你的 action 在仿真里 step，判成功/碰撞

你的 policy_server（提交 zip）

→ 只看 instruction + obs → 输出 7D action

→ 不跑环境、不读 BDDL




## 方案 A：指令启发式切 `n`（推荐先做）

在 `MyPolicy.reset(instruction)` 或 `__init__` 后根据语言设 `policy.config.n_action_steps`：

若指令匹配 long 模式（both / moka / microwave / and close / turn on ... and ...）

→ n = 10（或 8）

否则

→ n = 5

也可叠加：ACTION_EMA 仅对 long 打开

实现点：[`workspace/submission/policy_server.py`](vscode-file://vscode-app/Applications/Cursor.app/Contents/Resources/app/out/vs/code/electron-sandbox/workbench/workspace/submission/policy_server.py) 里现在只在启动时读一次 `N_ACTION_STEPS`；改成 每个 episode `reset` 时按 `instruction` 改 `self.policy.config.n_action_steps`，并清空 action 队列（`policy.reset()` 已有的话跟着重置）。

优点：不改权重、zip 一样交、可 A/B。  
缺点：靠关键词，Omni 措辞变了可能漏判（可再加「保守默认 n=5，只对明显多阶段加 n」）。

---

## 方案 B：双策略路由（真·特化）

短程权重 model_weights_short（n=5）

长程权重 model_weights_long（n=10，或 long-mix 再训）

reset 时选一个 MyPolicy 子模块 / 切换 MODEL

优点：容量分开，互不抢梯度。  
缺点：包体积×2、要维护两套训推、Omni 仍要靠路由启发式。

---

## 方案 C：训练侧条件化（更「模型内」）

- 数据里给长程任务更高采样权重（你们 complex 层已在做）
- 或把「horizon bin / 阶段」当额外条件（要改模型，重）
- 不要指望在语言里写 “use n=10” 会被当前架构执行

---

## 和「黑箱 VLA」的关系

VLA 仍然：V+L → 动作 chunk。  
特化的是外层控制器：这段任务执行多长开环、要不要 EMA、甚至用哪套权重。  
这不是教语言模型「记住 n」，而是 工程上的 task-conditioned execution。

---

## 建议

1. 先实现 方案 A（reset 时切 n，可选 EMA）
2. 本地：短程 4 example + 双壶/ketchup 多 seed
3. 再决定要不要方案 B 双权重

若要我直接改 `policy_server.py` 并写进操作手册，切换到 Agent 模式 说一声即可。



训练侧便宜增强（仍不采）

图像 color jitter / 亮度对比度等（若训练管线好接）。这是伪视觉扰动，补的是「外观变了还能跟」，不是新技能。

  

继续用好现有 14347

清洗、去坏轨、平衡难任务（双壶只有 146 ep）通常比「再采一波」更划算。
- 别以为 40 任务 = 扰动已经够：技能覆盖尚可；评测用的 texture/light 变体远多于训练元数据所表达的。
- 不必为了「加扰动」自己去采。
- 想加压：先 多跑已有扰动 BDDL；想训更稳：优先 数据清洗 + 难任务加权 +（可选）图像增广，而不是从零采集。
  
  
  
  
  
  你的观感和分数、轨迹指标是对得上的。先对齐一下官方题序（避免和口播错位）：

1. 木柜抽屉黑碗 → 盘
2. 番茄酱 → 篮子
3. 牛奶 → 篮子
4. 碗 → 炉灶

你说的「第二、三都去抓篮子」对应的就是 2+3；若你把番茄酱叫成牛奶，行为模式一样。

---

### 总判断

这不是「完全没输出 / 尺度炸了」那种坏权重（零样本才是 path≈8、jerk≈37）。  
LoRA 后 能动、更钝（path≈1.6、jerk≈2.5 vs Smol path≈1.1、jerk≈5），但 语义目标错、抓取相位塌掉 —— 典型是「看错/找错物体 + 只会做半截技能」。

根因仍主要落在：基座是 经典 LIBERO，LoRA 又 冻视觉、只训 expert，评测却是 LIBERO-plus 扰动。视觉表征跟不上 → 策略退回语言里的强词（basket / place）和粗糙接近。

---

### 分任务

1. 黑碗 / 抽屉

- 现象：抓不住、进不了「最上层抽屉」、你觉得 eef「太大」。
- 解读：更像 接近轨迹过粗 + 深度/抽屉层定位错（过冲或停在柜外），不一定是夹爪物理尺寸。抽屉题对视觉几何要求最高；冻视觉时最容易挂。
- 指标：同设定下 π0 path≈2.63（Smol≈1.15）—— 绕得更久仍完不成抓取。

2 & 3. 番茄酱 / 牛奶 → 篮子

- 现象：不去抓目标物，直接去够篮子。
- 解读：典型 目标顺序崩了（pick→place 合成失败）：指令里 `basket` 成了视觉/语言吸引子，策略跳过 grasp，直接执行「往篮子去」。
- 牛奶那两集还 碰撞=1，和「乱够篮子、蹭桌面物体」一致。
- 这和「漏了 preprocess」无关（你们已有 normalizer）；是 没学会「先谁后谁」。

4. 碗 → 炉灶

- 现象：碗没抓住，松手后机械臂在空中晃很久。
- 解读：抓取失败后仍按 place 相位 在开环播 chunk（`n=5`，chunk=50）。没有成功信号，也缺乏「没抓住就重试」的闭环，于是长时间无效运动直到 600 步。
- 「空白在干什么」= 时间相位错位后的空放动作，不是服务挂了。

---

### 和 Smol 成功时差在哪

||Smol 8k|你这版 π0|
|---|---|---|
|基座|已在 libero_plus 上训过|经典 libero + 薄 LoRA|
|视觉|随 plus 适配过|冻住|
|行为|先抓对物体再放|抓不住 / 直接奔篮子 / 空放|
|轨迹|短、干脆（~130 步成功）|满 600，path 不小但「做不对事」|

所以视频里看到的，不是偶发，而是 域偏移下的系统性技能分解失败。

---

### 若还要改 π0，优先试什么（仍勿交 Omni）

1. 别只加数据 steps：先让视觉能动（视觉 LoRA / 解冻），否则抽屉定位、物体 vs 篮子 很难好。
2. 推理消融：`ACTION_EMA=0`、`N_ACTION_STEPS=1~3` —— 减轻空放拖尾，但治不好「去抓篮子」。
3. 主线仍应是 Smol + plus；π0 需要 plus 向基座或更强视觉适配，才值得上 4×V100 重训。

如果你愿意，我可以再根据某一集（比如牛奶 ep00）对照 Smol 成功录像，按时间轴标「大约第几秒开始奔篮子」。


### 1. 「位置对不齐」主要是调视觉吗？

大体对，但不只有视觉。

|症状更像|更该动|
|---|---|
|看错物体 / 抓偏、伸到旁边、对抽屉层数不对|视觉（+ 后面把视觉特征变成动作的 action expert）|
|懂指令但手抖、轨迹糙、开合时机错|expert / 动作头，有时再加推理侧 EMA、`n_action_steps`|
|完全跑错任务语义（拿牛奶去干番茄酱的事）|才更像 语言 或指令对齐|

「位置」在仿真里 = 像素里的物体在哪 → 先靠 vision encoder；再由 expert 把视觉+状态变成 Δx/Δy/Δz。  
所以：对齐问题优先动视觉（你们 selective 已解）+ expert；解语言不是第一刀。  
你们抽屉碗失败，更像视觉/操控，不像「听不懂指令」。