### 一、 如何高效地“用 AI 辅助刷题”？

为了不落入“只抄代码不脑思考”的陷阱，建议遵循 **3 步提问法**：

1. **第一步：只让 AI 提供思路/提示（Hint）**
    
    - ❌ _“帮我写一下这题的 C++ / Python 代码。”_
        
    - _“这道题我思路卡住了，请不要直接给代码，只给我 3 个循序渐进的思路提示（Hint），并告诉我现在常用的解法算法是什么。”_
        
2. **第二步：自己写 / 让 AI 生成草稿后，你做“审查员”**
    
    - 当你想出思路后，可以尝试自己写，或者让 AI 生成一段初始代码，然后你自己去找漏洞（比如：“这里 $N=2\times 10^5$ 会不会超时？”、“极小值 0 会不会报错？”）。
        
3. **第三步：让 AI 针对代码做性能与复杂度优化**
    
    - _“这段代码虽然通过了，但它的时间复杂度和空间复杂度是多少？在 C++ 中有没有更优雅或更常用于竞赛的写法？”_
        

---

### 二、 有必要用本地 IDE / IDLE 刷题吗？

**结论：阶段性有必要，但初期不需要把它作为硬性负担。**

#### 1. 前期（灰名~绿名，Rating < 1000）：**没必要强求本地 IDE**

- **理由**：这个阶段的核心瓶颈是**逻辑理解、算法思想和快速验证**。
    
- **做法**：直接在 **AtCoder 网页自带的编辑框**，或者利用 **AtCoder Problems / LeetCode 的在线编辑器**直接写代码提交即可。在线平台会自动编译、跑样例，反馈速度最快。
    

#### 2. 中后期（冲刺水色，Rating 1000~1200+）：**非常有必要建立顺手的本地/云端 IDE 流程**

当你开始做 C、D、E 题时，本地 IDE（如 **VS Code** 或 **CLion**）的优势会非常明显：

- **本地 Debug / 断点调试**：当出现诡异的错误（如 `Segmentation Fault` 或某一步数组越界）时，网页端只会返回一个错误代码，而本地 IDE 能让你一步步单步执行，直观看到变量变化。
    
- **本地测试样例（Testcase）速度更快**：许多竞赛选手会在 VS Code 中配置插件（如 `Competitive Companion` 或 `AtCoder CLI`），按一个快捷键就能自动拉取题目所有的 Sample 数据并在本地秒跑。
    
- **模版与快捷补全**：在比赛争分夺秒时，本地 IDE 可以帮你自动插入常用的竞赛 Header（如 C++ 的 `#include <bits/stdc++.h>` 或常用的预定义模版）。