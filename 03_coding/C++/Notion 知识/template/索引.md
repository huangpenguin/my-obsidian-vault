---
title: "template"
publish: false
tags: ["C++"]
---
# template

# 函数模板

不要把这里的class与类的声明关键字class混淆在一起，虽然它们由相同的字母组成，但含义是不同的。这里的class表示T是一个类型参数，可以是任何数据类型，如int、float、char等,为了以示区分还可以是使用typename关键字

```cpp
template <class T1, class T2,…>//到此为止都是模板的代替的部分，下一行就是函数不变的部分
返回类型 函数名(参数表)
{...}
template <typename T1, typename T2,…>
返回类型 函数名(参数表)
{...}
template <class T1,class T2,class T3,int T4>
T1 fx(T1 a, T2 b, T3 c)
{...}
```

### 实例化发生的时机

- 模板实例化发生在调用模板函数时。当编译器遇到程序中对函数模板的调用时，它才会根据调用语句中实参的具体类型，确定模板参数的数据类型，并用此类型替换函数模板中的模板参数，生成能够处理该类型的函数代码，即模板函数。
- 当多次发生类型相同的参数调用时，只在第1次进行实例化。假设有下面的函数调用：

```
int x=fx(2,3);
int y=fx(3,9);
int z=fx(8.5);
```

编译器只在第1次调用时生成模板函数，当之后遇到相同类型的参数调用时，不再生成其他模板函数，它将调用第1次实例化生成的模板函数。

```cpp
#include <iostream>

// 定义一个函数模板
template <typename T>
T add(T a, T b) {
    return a + b;
}

int main() {
    int result1 = add(5, 10); // 编译器根据参数类型推断 T 为 int
    double result2 = add(3.5, 2.7); // 编译器根据参数类型推断 T 为 double
    
    std::cout << "Result 1: " << result1 << std::endl;
    std::cout << "Result 2: " << result2 << std::endl;
    
    return 0;
}

```

# 类模板

```cpp
#include <iostream>

// 定义一个类模板
template <typename T>
class Pair {
private:
    T first;
    T second;
public:
    Pair(T f, T s) : first(f), second(s) {}
    
    T getFirst() const {
        return first;
    }
    
    T getSecond() const {
        return second;
    }
};

int main() {
    Pair<int> pair1(1, 2); // Pair<int> 实例化一个存储 int 类型的 Pair
    Pair<double> pair2(3.5, 4.7); // Pair<double> 实例化一个存储 double 类型的 Pair
    
    std::cout << "Pair 1: " << pair1.getFirst() << ", " << pair1.getSecond() << std::endl;
    std::cout << "Pair 2: " << pair2.getFirst() << ", " << pair2.getSecond() << std::endl;
    
    return 0;
}

```

通过使用模板，C++ 标准库中的容器类如 `vector` 和 `map` 提供了一种通用的数据结构实现方式

[vector](vector.md)

[map](map.md)

所以其实模板就是用小尖尖括号里的代替值去代替模板中的未知数
