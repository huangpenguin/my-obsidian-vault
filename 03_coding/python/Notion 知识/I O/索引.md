---
title: "I/O"
publish: false
tags: ["Python"]
---
# I/O

```python
fileobj=open(filename,mode)
'''
mode 
1st parameter
r read
w write no matter existed or not
x write only when not existed
a append

2st parameter
t text(default)
b binary 
'''

#print()
fout=open('abc.txt','w')
print('a',file=fout,sep='',end='')
fout.close()

fout=open('abc.txt','w')
fout.write(poem)
fout.close()

#最好的实践
try:
	fout=open('relativity',x)
	fout.write('a')
except FileExistsError:
	print('existed')
```

```python
#read files
poem=''
fin=open('relativity','r')
chunk=100
while True:
	fragment=fin.read(chunk)
	if not fragment:
		break
	poem+=fragment
fin.close()

#每次读一行
poem=''
fin=open('relativity','r')
while True:
	line=fin.readline()
	if not line:
		break
	poem+=line
fin.close()

#使用iterator
poem=''
fin=open('relativity','r')
for line in fin:#fin是迭代器
	poem+=line
fin.close()

#使用readlines
#这里与上面三个方法不同的是,这里返回的是一个列表
fin=open('relativity','r')
lines=fin.readlines()
fin.close()
```

```python
#write files

fout=open('bfile','wb')
size=len(bdata)
offset=0
chunk=100
while True:
	if offset>size:
		break
	fout.write(bdata[offset:offset+chunk])
	offset+=chunk
```

```python
with open('relativity','w') as fout:
    fout.write(poem)
```

```python
#tell()
fin=open('bfile','rb')
fin.tell()
#seek(offset,mode)
fin.seek(-1,2)#原理是调整游标
'''
seek()的mode
0 从头开始
1 从现在开始
2 从末尾（右）开始
'''
```

```python
import os
os.path.exists('abc.txt')#bool
```

.表示当前文件夹，两个点..表示上一级文件夹

```python
import shutil
shutil.copy('a.txt','b.txt')
shutil.move('a.txt','b.txt')//remove the past one
```

```python
#link simlink
os.link('a.txt','b.txt')
os.path.isfile('b.txt')#yes
os.path.islink('b.txt')#no
os.symlink('a.txt','b.txt')
os.path.islink('b.txt')#yes
```

# Directory操作

```python
import os
os.mkdir('poems')
os.path.exists('poems')

os.rmdir('poems')
os.path.exists('poems')

#scan the path
with os.scandir("poems")as it:
	entries=[entry.name for entry in it]

#change current directory
os.chdir('poems')
with os.scandir() as it:
	entries=[entry.name for entry in it]

#directory match
import glob
glob.glob('m*')

#path
win_file='a\\b\\c'
win_file2=r'a\b\c'

os.path.abspath('a.txt')
os.path.realpath('a.txt')#之前的symlink的反向操作

win_file=os.path.join(win_file,'.xxx')
```

# Pathlib

[pathlib explaination](pathlib%20explaination/%E7%B4%A2%E5%BC%95.md)

```python
from pathlib import Path
file_path=Path('a')/'b.txt'/'c,txt'
```

[glob](glob.md)

# BytesIO and StringIO(file-like object)

### `BytesIO` 示例

`BytesIO` 用于处理二进制数据：

```python
from io import BytesIO

# 创建一个BytesIO对象
bytes_io = BytesIO()

# 向BytesIO对象写入二进制数据
bytes_io.write(b'This is some binary data.')

# 移动到流的开头
bytes_io.seek(0)

# 读取数据
data = bytes_io.read()
print(data)  # 输出: b'This is some binary data.'

# 关闭BytesIO对象
bytes_io.close()

```

### `StringIO` 示例

`StringIO` 用于处理文本数据：

```python
from io import StringIO

# 创建一个StringIO对象
string_io = StringIO()

# 向StringIO对象写入文本数据
string_io.write('This is some text data.')

# 移动到流的开头
string_io.seek(0)

# 读取数据
data = string_io.read()
print(data)  # 输出: This is some text data.

# 关闭StringIO对象
string_io.close()

```

这两个示例展示了如何使用 `BytesIO` 和 `StringIO` 来处理内存中的二进制数据和文本数据。`BytesIO` 常用于处理图像、音频等二进制数据，而 `StringIO` 则常用于处理字符串数据。

`StringIO` 是一个类，用于在内存中读写字符串数据。它模拟了文件对象的行为，因此具有类似于文件对象的方法，比如 `read`、`write`、`seek` 等。这使得 `StringIO` 对象可以像文件一样进行操作，但实际上数据是存储在内存中的，而不是在磁盘上。

### `StringIO` 对象的行为

由于 `StringIO` 模拟了文件对象，所以你可以使用 `read` 方法来读取数据，类似于从文件中读取数据。

以下是一些关键方法和属性：

- `write(s)`: 将字符串 `s` 写入 `StringIO` 对象。
- `read(size=-1)`: 从 `StringIO` 对象读取数据，`size` 是要读取的字符数。如果不指定 `size`，则读取所有数据。
- `seek(pos, whence=0)`: 移动到流中的一个新位置。`pos` 是偏移量，`whence` 决定了偏移的起点（0：从流的开头，1：从当前位置，2：从流的结尾）。
- `getvalue()`: 获取流中的所有内容。

---

### 使用 [`json`](https://docs.python.org/zh-cn/3.12/library/json.html#module-json) 保存结构化数据

字符串可以很容易地写入文件或从文件中读取。 数字则更麻烦一些，因为 [`read()`](https://docs.python.org/zh-cn/3.12/library/io.html#io.TextIOBase.read) 方法只返回字符串，而字符串必须传给 [`int()`](https://docs.python.org/zh-cn/3.12/library/functions.html#int) 这样的函数，它接受 `'123'` 这样的字符串并返回其数值 123。 当你想要保存嵌套列表和字典等更复杂的数据类型时，手动执行解析和序列化操作将会变得非常复杂。

Python 允许你使用流行的数据交换格式 [JSON (JavaScript Object Notation)](https://json.org/)，而不是让用户持续编写和调试代码来将复杂的数据类型存入文件中。 标准库模块 [`json`](https://docs.python.org/zh-cn/3.12/library/json.html#module-json) 可以接受带有层级结构的 Python 数据，并将其转换为字符串表示形式；这个过程称为 *serializing*。 根据字符串表示形式重建数据则称为 *deserializing*。 在序列化和反序列化之间，用于代表对象的字符串可以存储在文件或数据库中，或者通过网络连接发送到远端主机。

**备注**

JSON 格式通常用于现代应用程序的数据交换。程序员早已对它耳熟能详，可谓是交互操作的不二之选。

只需一行简单的代码即可查看某个对象的 JSON 字符串表现形式：

```python
import json
x = [1, 'simple', 'list']
json.dumps(x)
'[1, "simple", "list"]'
```

[`dumps()`](https://docs.python.org/zh-cn/3.12/library/json.html#json.dumps) 函数还有一个变体， [`dump()`](https://docs.python.org/zh-cn/3.12/library/json.html#json.dump) ，它只将对象序列化为 [text file](https://docs.python.org/zh-cn/3.12/glossary.html#term-text-file) 。因此，如果 `f` 是 [text file](https://docs.python.org/zh-cn/3.12/glossary.html#term-text-file) 对象，可以这样做：

`json.dump(x, f)`

要再次解码对象，如果 `f` 是已打开、供读取的 [binary file](https://docs.python.org/zh-cn/3.12/glossary.html#term-binary-file) 或 [text file](https://docs.python.org/zh-cn/3.12/glossary.html#term-text-file) 对象：

`x = json.load(f)`

**备注**

JSON文件必须以UTF-8编码。当打开JSON文件作为一个 [text file](https://docs.python.org/zh-cn/3.12/glossary.html#term-text-file) 用于读写时，使用 `encoding="utf-8"` 。

这种简单的序列化技术可以处理列表和字典，但在 JSON 中序列化任意类的实例，则需要付出额外努力。[`json`](https://docs.python.org/zh-cn/3.12/library/json.html#module-json) 模块的参考包含对此的解释。

**参见**

[`pickle`](https://docs.python.org/zh-cn/3.12/library/pickle.html#module-pickle) - 封存模块

与 [JSON](https://docs.python.org/zh-cn/3.12/tutorial/inputoutput.html#tut-json) 不同，*pickle* 是一种允许对复杂 Python 对象进行序列化的协议。因此，它为 Python 所特有，不能用于与其他语言编写的应用程序通信。默认情况下它也是不安全的：如果解序化的数据是由手段高明的攻击者精心设计的，这种不受信任来源的 pickle 数据可以执行任意代码。
